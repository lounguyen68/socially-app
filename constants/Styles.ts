import { ViewStyle } from 'react-native';

export const defaultStyles = {
  iconContainer: {
    alignItems: 'center',
    justifyContent: 'center',
  } as ViewStyle,
  buttonContainer: {
    backgroundColor: '#FFF',
    alignItems: 'center',
    justifyContent: 'center',
  } as ViewStyle,
};
